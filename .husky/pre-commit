# Run lint-staged
yarn lint-staged

# Type check staged TypeScript/Svelte files
if git diff --cached --name-only | grep -E '\.(ts|svelte)$' > /dev/null; then
  echo "🔍 Type checking staged files..."
  yarn check-types || exit 1
fi
